<ContentControl x:Class="WpfControlSamples.Views.Menus.FileDialogPage"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:local="clr-namespace:WpfControlSamples.Views.Menus"
                mc:Ignorable="d" 
                d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="10" >
        <TextBlock Margin="0,10,0,0"
                   FontSize="{StaticResource FontSizeMedium}"
                   Text="OpenFileDialog / SaveFileDialog" />

        <Button Height="40" Content="FileOpen" Click="FileOpenButton_Click" />
        <Button Height="40" Content="FileSave" Click="FileSaveButton_Click" />
        
        <TextBlock Margin="10"
                   TextWrapping="Wrap"
                   FontSize="{StaticResource FontSizeMicro}"
                   Text="{Binding ., Mode=OneWay}" />
        
    </StackPanel>

</ContentControl>
